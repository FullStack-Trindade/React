import React from 'react';

function Button({prop1, prop2, prop3}) {
        
    return (  
        <React.Fragment>
            <button>{prop1}</button>
            
        </React.Fragment>
    );
}

export default Button;